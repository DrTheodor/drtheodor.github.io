---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"canvas">;

const { class: className, ...props } = Astro.props;
---

<canvas
    class:list={[className, "w-full h-full absolute top-0 left-0 opacity-0"]}
    {...props}></canvas>
